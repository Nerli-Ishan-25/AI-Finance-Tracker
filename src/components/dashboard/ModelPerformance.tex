import React from 'react';

export function ModelPerformance() {
  const models = [
    { name: 'Gradient Boosting', accuracy: 94.2, precision: 91.8, recall: 89.5, f1: 90.6 },
    { name: 'Random Forest', accuracy: 92.7, precision: 89.3, recall: 91.2, f1: 90.2 },
    { name: 'Logistic Regression', accuracy: 88.9, precision: 86.7, recall: 88.1, f1: 87.4 },
  ];

  return (
    <div className="bg-white rounded-lg shadow-sm border">
      <div className="p-6">
        <h3 className="text-lg font-semibold text-gray-900 mb-4">Model Performance Metrics</h3>
        
        <div className="space-y-4">
          {models.map((model, index) => (
            <div key={index} className="p-4 bg-gray-50 rounded-lg">
              <div className="flex items-center justify-between mb-3">
                <h4 className="font-medium text-gray-900">{model.name}</h4>
                <span className={`px-2 py-1 text-xs rounded-full ${
                  index === 0 ? 'bg-green-100 text-green-800' : 'bg-blue-100 text-blue-800'
                }`}>
                  {index === 0 ? 'Active' : 'Backup'}
                </span>
              </div>
              
              <div className="grid grid-cols-4 gap-4">
                <div className="text-center">
                  <div className="text-lg font-semibold text-gray-900">{model.accuracy}%</div>
                  <div className="text-sm text-gray-500">Accuracy</div>
                </div>
                <div className="text-center">
                  <div className="text-lg font-semibold text-gray-900">{model.precision}%</div>
                  <div className="text-sm text-gray-500">Precision</div>
                </div>
                <div className="text-center">
                  <div className="text-lg font-semibold text-gray-900">{model.recall}%</div>
                  <div className="text-sm text-gray-500">Recall</div>
                </div>
                <div className="text-center">
                  <div className="text-lg font-semibold text-gray-900">{model.f1}%</div>
                  <div className="text-sm text-gray-500">F1-Score</div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}