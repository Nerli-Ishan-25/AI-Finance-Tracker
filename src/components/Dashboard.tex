import React from 'react';
import { RiskMetrics } from './dashboard/RiskMetrics';
import { RecentAssessments } from './dashboard/RecentAssessments';
import { ModelPerformance } from './dashboard/ModelPerformance';
import { ComplianceStatus } from './dashboard/ComplianceStatus';

export function Dashboard() {
  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h1 className="text-3xl font-bold text-gray-900">Financial Risk Analysis Dashboard</h1>
        <div className="text-sm text-gray-500">
          Last updated: {new Date().toLocaleString()}
        </div>
      </div>

      <RiskMetrics />
      
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <RecentAssessments />
        <ModelPerformance />
      </div>
      
      <ComplianceStatus />
    </div>
  );
}