import React from 'react';
import { Clock, User, Activity } from 'lucide-react';

export function AuditTrail() {
  const auditEvents = [
    {
      timestamp: '2024-01-15 14:32:15',
      user: 'system',
      action: 'Model Prediction',
      details: 'Risk assessment completed for application LA-2024-001',
      severity: 'info',
    },
    {
      timestamp: '2024-01-15 14:30:22',
      user: 'john.officer@bank.com',
      action: 'Manual Override',
      details: 'Risk score manually adjusted for application LA-2024-002 due to additional collateral',
      severity: 'warning',
    },
    {
      timestamp: '2024-01-15 14:25:10',
      user: 'system',
      action: 'Compliance Check',
      details: 'ECOA bias monitoring passed for batch processing',
      severity: 'info',
    },
    {
      timestamp: '2024-01-15 14:20:05',
      user: 'system',
      action: 'Data Access',
      details: 'Credit bureau data retrieved for customer verification',
      severity: 'info',
    },
    {
      timestamp: '2024-01-15 14:15:33',
      user: 'admin@bank.com',
      action: 'Model Update',
      details: 'Gradient Boosting model retrained with new data batch',
      severity: 'critical',
    },
  ];

  const getSeverityColor = (severity: string) => {
    switch (severity) {
      case 'critical':
        return 'text-red-600 bg-red-100';
      case 'warning':
        return 'text-yellow-600 bg-yellow-100';
      case 'info':
        return 'text-blue-600 bg-blue-100';
      default:
        return 'text-gray-600 bg-gray-100';
    }
  };

  return (
    <div className="bg-white rounded-lg shadow-sm border">
      <div className="p-6">
        <div className="flex items-center space-x-2 mb-6">
          <Activity className="h-5 w-5 text-blue-600" />
          <h2 className="text-xl font-semibold text-gray-900">Audit Trail</h2>
        </div>

        <div className="space-y-4">
          {auditEvents.map((event, index) => (
            <div key={index} className="flex items-start space-x-4 p-4 bg-gray-50 rounded-lg">
              <div className="flex-shrink-0 mt-1">
                <Clock className="h-4 w-4 text-gray-400" />
              </div>
              
              <div className="flex-1 min-w-0">
                <div className="flex items-center justify-between mb-1">
                  <div className="flex items-center space-x-2">
                    <User className="h-4 w-4 text-gray-400" />
                    <span className="text-sm font-medium text-gray-900">{event.user}</span>
                  </div>
                  <span className={`px-2 py-1 text-xs rounded-full font-medium ${getSeverityColor(event.severity)}`}>
                    {event.severity}
                  </span>
                </div>
                
                <div className="text-sm text-gray-600 mb-1">{event.action}</div>
                <div className="text-sm text-gray-500">{event.details}</div>
                <div className="text-xs text-gray-400 mt-2">{event.timestamp}</div>
              </div>
            </div>
          ))}
        </div>

        <div className="mt-6 p-4 bg-blue-50 rounded-lg">
          <h4 className="font-medium text-blue-900 mb-2">Retention Policy</h4>
          <p className="text-sm text-blue-800">
            Audit logs are retained for 7 years as required by banking regulations. 
            All system actions and user interactions are logged with cryptographic integrity protection.
          </p>
        </div>
      </div>
    </div>
  );
}