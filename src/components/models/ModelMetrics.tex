import React from 'react';
import { Activity, Target, Zap } from 'lucide-react';

export function ModelMetrics() {
  const models = [
    {
      name: 'Gradient Boosting',
      status: 'Production',
      accuracy: 94.2,
      precision: 91.8,
      recall: 89.5,
      f1Score: 90.6,
      auc: 0.946,
      trainingTime: '2.3 hours',
      predictionTime: '12ms',
    },
    {
      name: 'Random Forest',
      status: 'Backup',
      accuracy: 92.7,
      precision: 89.3,
      recall: 91.2,
      f1Score: 90.2,
      auc: 0.928,
      trainingTime: '45 minutes',
      predictionTime: '8ms',
    },
    {
      name: 'Logistic Regression',
      status: 'Baseline',
      accuracy: 88.9,
      precision: 86.7,
      recall: 88.1,
      f1Score: 87.4,
      auc: 0.891,
      trainingTime: '5 minutes',
      predictionTime: '2ms',
    },
  ];

  const getStatusColor = (status: string) => {
    switch (status) {
      case 'Production': return 'bg-green-100 text-green-800';
      case 'Backup': return 'bg-blue-100 text-blue-800';
      case 'Baseline': return 'bg-gray-100 text-gray-800';
      default: return 'bg-gray-100 text-gray-800';
    }
  };

  return (
    <div className="space-y-6">
      {models.map((model, index) => (
        <div key={index} className="bg-white rounded-lg shadow-sm border">
          <div className="p-6">
            <div className="flex items-center justify-between mb-6">
              <div className="flex items-center space-x-3">
                <Activity className="h-6 w-6 text-blue-600" />
                <div>
                  <h3 className="text-xl font-semibold text-gray-900">{model.name}</h3>
                  <span className={`inline-block px-2 py-1 text-xs rounded-full font-medium ${getStatusColor(model.status)}`}>
                    {model.status}
                  </span>
                </div>
              </div>
              
              <div className="text-right">
                <div className="text-2xl font-bold text-gray-900">{model.accuracy}%</div>
                <div className="text-sm text-gray-500">Overall Accuracy</div>
              </div>
            </div>

            <div className="grid grid-cols-2 md:grid-cols-4 gap-6 mb-6">
              <div className="text-center">
                <div className="text-lg font-semibold text-gray-900">{model.precision}%</div>
                <div className="text-sm text-gray-500">Precision</div>
              </div>
              <div className="text-center">
                <div className="text-lg font-semibold text-gray-900">{model.recall}%</div>
                <div className="text-sm text-gray-500">Recall</div>
              </div>
              <div className="text-center">
                <div className="text-lg font-semibold text-gray-900">{model.f1Score}%</div>
                <div className="text-sm text-gray-500">F1-Score</div>
              </div>
              <div className="text-center">
                <div className="text-lg font-semibold text-gray-900">{model.auc}</div>
                <div className="text-sm text-gray-500">AUC-ROC</div>
              </div>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-2 gap-6 pt-6 border-t">
              <div className="flex items-center space-x-3">
                <Target className="h-5 w-5 text-gray-400" />
                <div>
                  <div className="text-sm text-gray-500">Training Time</div>
                  <div className="font-medium text-gray-900">{model.trainingTime}</div>
                </div>
              </div>
              
              <div className="flex items-center space-x-3">
                <Zap className="h-5 w-5 text-gray-400" />
                <div>
                  <div className="text-sm text-gray-500">Prediction Time</div>
                  <div className="font-medium text-gray-900">{model.predictionTime}</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>
  );
}